Campo de texto de todos los trámites del portal.

```jsx
<Input label="RUT" prefix="RUT" placeholder="12.345.678-9" required />
```

- El foco pinta borde morado suave + halo de 3px con el tinte de marca.
- `error` reemplaza a `hint` y vuelve rojo el borde; escribe el mensaje en español, sentence case, sin signos de exclamación.
